---
import { Icon } from 'astro-icon/components';
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_TITLE } from '../consts';

// Define your CV data - customize these with your own information
const experience = [
  {
    title: 'Senior Software Engineer',
    company: 'Company Name',
    period: '2022 - Present',
    description:
      'Description of your role and key achievements. Highlight impactful projects and technologies used.',
    technologies: ['TypeScript', 'React', 'Node.js', 'AWS'],
  },
  {
    title: 'Software Engineer',
    company: 'Previous Company',
    period: '2019 - 2022',
    description:
      'Description of your role and accomplishments. Focus on growth and meaningful contributions.',
    technologies: ['JavaScript', 'Vue.js', 'Python', 'PostgreSQL'],
  },
  {
    title: 'Junior Developer',
    company: 'First Company',
    period: '2017 - 2019',
    description: 'Started your career here. Mention foundational skills learned and early wins.',
    technologies: ['HTML', 'CSS', 'JavaScript', 'PHP'],
  },
];

const education = [
  {
    degree: 'M.S. Computer Science',
    institution: 'University Name',
    period: '2015 - 2017',
    description: 'Specialized in distributed systems. Thesis on real-time data processing.',
  },
  {
    degree: 'B.S. Computer Science',
    institution: 'University Name',
    period: '2011 - 2015',
    description: 'Graduated with honors. Focus on software engineering and algorithms.',
  },
];

const skills = {
  Languages: ['TypeScript', 'JavaScript', 'Python', 'Go', 'Rust'],
  Frontend: ['React', 'Vue.js', 'Astro', 'Tailwind CSS', 'Next.js'],
  Backend: ['Node.js', 'FastAPI', 'PostgreSQL', 'Redis', 'GraphQL'],
  DevOps: ['Docker', 'Kubernetes', 'AWS', 'Cloudflare', 'GitHub Actions'],
};
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead title={`CV | ${SITE_TITLE}`} description="My professional experience and skills" />
  </head>
  <body class="min-h-screen flex flex-col">
    <Header />
    <main class="container mx-auto px-4 py-8 max-w-4xl flex-1">
      <h1 class="text-4xl font-bold mb-2">Curriculum Vitae</h1>
      <p class="text-base-content/70 mb-8">My professional journey and skills</p>

      <!-- Experience Section -->
      <section class="mb-12">
        <h2 class="text-2xl font-bold mb-6 flex items-center gap-2">
          <Icon name="tabler:briefcase" class="h-6 w-6" />
          Experience
        </h2>

        <ul class="timeline timeline-snap-icon max-md:timeline-compact timeline-vertical">
          {
            experience.map((job, index) => (
              <li>
                {index > 0 && <hr class="bg-primary" />}
                <div class="timeline-middle">
                  <Icon name="tabler:circle-check" class="h-5 w-5 text-primary" />
                </div>
                <div
                  class={`timeline-${index % 2 === 0 ? 'start' : 'end'} mb-10 ${index % 2 === 0 ? 'md:text-end' : ''}`}
                >
                  <time class="font-mono italic text-sm">{job.period}</time>
                  <div class="text-lg font-black">{job.title}</div>
                  <div class="text-primary font-semibold">{job.company}</div>
                  <p class="mt-2 text-base-content/80">{job.description}</p>
                  <div
                    class={`flex flex-wrap gap-2 mt-3 ${index % 2 === 0 ? 'md:justify-end' : ''}`}
                  >
                    {job.technologies.map((tech) => (
                      <span class="badge badge-sm badge-outline">{tech}</span>
                    ))}
                  </div>
                </div>
                {index < experience.length - 1 && <hr class="bg-primary" />}
              </li>
            ))
          }
        </ul>
      </section>

      <!-- Education Section -->
      <section class="mb-12">
        <h2 class="text-2xl font-bold mb-6 flex items-center gap-2">
          <Icon name="tabler:school" class="h-6 w-6" />
          Education
        </h2>

        <ul class="timeline timeline-snap-icon max-md:timeline-compact timeline-vertical">
          {
            education.map((edu, index) => (
              <li>
                {index > 0 && <hr class="bg-secondary" />}
                <div class="timeline-middle">
                  <Icon name="tabler:circle-check" class="h-5 w-5 text-secondary" />
                </div>
                <div
                  class={`timeline-${index % 2 === 0 ? 'start' : 'end'} mb-10 ${index % 2 === 0 ? 'md:text-end' : ''}`}
                >
                  <time class="font-mono italic text-sm">{edu.period}</time>
                  <div class="text-lg font-black">{edu.degree}</div>
                  <div class="text-secondary font-semibold">{edu.institution}</div>
                  <p class="mt-2 text-base-content/80">{edu.description}</p>
                </div>
                {index < education.length - 1 && <hr class="bg-secondary" />}
              </li>
            ))
          }
        </ul>
      </section>

      <!-- Skills Section -->
      <section>
        <h2 class="text-2xl font-bold mb-6 flex items-center gap-2">
          <Icon name="tabler:bulb" class="h-6 w-6" />
          Skills
        </h2>

        <div class="grid md:grid-cols-2 gap-6">
          {
            Object.entries(skills).map(([category, skillList]) => (
              <div class="card bg-base-200">
                <div class="card-body">
                  <h3 class="card-title text-lg">{category}</h3>
                  <div class="flex flex-wrap gap-2">
                    {skillList.map((skill) => (
                      <span class="badge badge-primary">{skill}</span>
                    ))}
                  </div>
                </div>
              </div>
            ))
          }
        </div>
      </section>
    </main>
    <Footer />
  </body>
</html>
